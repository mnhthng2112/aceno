<html>
    <head>
        {{ content_for_header }}
        {{ "style.css" | asset_url | stylesheet_tag }}
    </head>
    <body>
        {{ content_for_layout }}
        {% render 'header' %}
        {{ "script.js" | asset_url | script_tag }}
    </body>
</html>